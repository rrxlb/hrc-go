# Minimal Dockerfile - use this if the main Dockerfile fails
FROM golang:1.21-alpine

# Install git (needed for go mod download)
RUN apk add --no-cache git ca-certificates

# Set working directory
WORKDIR /app

# Copy everything
COPY . .

# Build directly without separate download step
RUN go build -o main .

# Expose port
EXPOSE 8080

# Run the binary
CMD ["./main"]